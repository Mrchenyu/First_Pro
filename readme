# run environment gcc 5.4.0 on Ubuntu 5.4.0

# use list to store sutdent and support add, del, reverse, traverse and sort function 
# just simple project to practice code ability

# cd src/ && make
# ./studentlist
